{{!-- 
    Support for Shopify collections
    https://shopify.dev/docs/storefront-api/reference/object/collection
    - No collections
    - No products in collection
 --}}
{
    "totalCount": {{faker "number.int" min=1 max=100}},
    "pageInfo": {{> cursor noPreviousPage=true}},
    "edges": [

        {{#unless (get path="ms-collections-no-collection" context=context.headers)}}
            {{#repeat (select context.tree.collections.__args.first 10)}}
                    {
                        "cursor": "{{faker "string.alphanumeric" 5}}",
                        "node": {{> collection products_total=(select context.tree.collection.products.__args.first 10)}}
                    }
            {{/repeat}}
        {{/unless}}
    ],
    "nodes": [
        {{#unless (get path="ms-collections-no-collection" context=context.headers)}}
            {{#repeat (select context.tree.collections.__args.first 10)}}
                {{> collection products_total=(select context.tree.collection.products.__args.first 10)}}
            {{/repeat}}
        {{/unless}}
    ]
}